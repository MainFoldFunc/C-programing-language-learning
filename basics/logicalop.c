#include <stdio.h>
#include <stdbool.h>
int main (){


	float temp;
	bool snowing = true;
	if (temp >= 0 && snowing){
		printf("Its cold and snowing\n");
	}
	else if (temp <= ! snowing){
		printf("It is cold but it/s not snowing\n");

	}
	
	return 0;
}
